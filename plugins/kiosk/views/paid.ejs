		<%
			var formatMoney = function (n, c, d, t) {
				var c = isNaN(c = Math.abs(c)) ? 2 : c, d = d == undefined ? "," : d, t = t == undefined ? "." : t, s = n < 0 ? "-" : "", i = parseInt(n = Math.abs(+n || 0).toFixed(c)) + "", j = (j = i.length) > 3 ? j % 3 : 0;
				return s + (j ? i.substr(0, j) + t : "") + i.substr(j).replace(/(\d{3})(?=\d)/g, "$1" + t) + (c ? d + Math.abs(n - i).toFixed(c).slice(2) : "");
			};

			var displayBalance = formatMoney(balance / 100, 2, '.', "'");

			var posneg = (balance < 0) ? 'negative' : 'positive';
		%>
<div class="row">
	<div class="span12">
		<div class="page-header">
			<h1>Thank You!</h1>
			<p>You just bought <strong><%= item.name %></strong> for CHF <%= formatMoney(item.value / 100, 2, '.', "'") %></p>
		</div>
	</div>
</div>

<div class="row">
	<div class="span12">
		<p text-align: center;>Your new account balance is: </p>
	</div>

	<div class="span12">
		<div class="well">
			<p class="balance <%= posneg %>"><%= displayBalance %></p>
		</div>
	</div>
</div>

<div class="row" style="margin-top: 36px; margin-bottom: 36px;">
	<div class="span12" style="text-align: center;">
		<a class="btn btn-large" href="/">Home</a>
		<a class="btn btn-large" href="/account">View account</a>
		<a style="display:inline-block; margin-left: 18px;" href="/about"><img src="/img/irma.svg" style="height: 47px;" /></a>
	</div>
</div>
